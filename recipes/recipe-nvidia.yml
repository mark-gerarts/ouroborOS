---
name: ouroboros-nvidia
description: OuroborOS, a custom ublue spin (nvidia drivers included).

base-image: ghcr.io/ublue-os/silverblue-main
image-version: latest

modules:
    - from-file: common.yml
